---
const base = import.meta.env.BASE_URL;

const {
  label,
  ref = "reserva",
  colorScheme = "primary",
  className,
  anchor = false,
} = Astro.props;
---

<a
  href={anchor ? `${ref}` : `${base}/${ref}`}
  class={`btn-default btn-${colorScheme} ${className}`}>{label}</a
>
